//! PTY helpers — placeholder module for future PTY management
//! utilities (resize, signal forwarding, etc.).
//!
//! For MVP, all PTY interaction is handled directly in
//! [`crate::TerminalPane::spawn`]. This module exists so the crate
//! structure is ready for expansion.
